# 4월 30일  C++ 연습

### 문제 7-5

메인 함수의 ~a; ~b; ~c; 라는 코드가 있는데 ~를 썼길래 소멸자 함수를 실행한 줄 알았습니다. 하지만 클래스 이름이 Light이기 때문에 소멸자를 실행하려면 a.~Light();가 맞는 코드입니다. 그래서 ~라는 새로운 연산자 함수를 만들어야 된다는 것을 생각해냈습니다. 이후 ~가 비트 반전 연산자로 존재 한다는 것을 알았습니다.

