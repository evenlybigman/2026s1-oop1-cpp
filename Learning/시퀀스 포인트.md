시퀀스 포인트 

정의 : 어떤 계산이 완전히 끝났는지 구분해 주는 기준점

종류 : ";"  ","  "&&", "||" 등등..



```c언어
printf("%d %d\n", x++, ++x);
```

이 코드가 잘못된 이유는 시퀀스 포인트가 없기 때문에 x++과 ++x의 평가 순서가 정해져 있지 않음.

-> 컴파일러에 따라 순서가 다를 수 있고 오류나 크러쉬가 날 수도 있음.

값을 여러번 변경할때는 시퀀스 포인트를 사용해서 변경을 해야한다.